#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main()
{
   // printf("fork 之前: 我是一个进程: pid: %d, ppid: %d\n",getpid(),getppid());

   // fork();

   // printf("fork 之后: 我是一个进程: pid: %d, ppid: %d\n",getpid(),getppid());

   // sleep(1);

   // while(1)
   // {
   //     printf("我是一个进程: pid: %d, ppid: %d\n",getpid(),getppid());
   //     sleep(1);
   // }
    return 0;
}
